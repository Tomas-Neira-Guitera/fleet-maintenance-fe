VITE_API_BASE_URL=http://localhost:8080
# Mientras el backend de inspecciones no esté listo, los servicios usan datos
# mock en memoria (ver src/services). Poner en "false" cuando la API real
# exista para que los mismos servicios pasen a hacer fetch() contra ella.
VITE_USE_MOCKS=true
