# FleetGuard — frontend (`fleet-maintenance-fe`)

@AGENTS.md

El contexto del proyecto, el contrato de API y el estado de avance viven en el
repo del backend, en la carpeta hermana `TIP - Backend`:

- `../TIP - Backend/docs/API.md` — **leelo antes de escribir cualquier `fetch`**
- `../TIP - Backend/docs/PROJECT.md` — qué es FleetGuard y por qué está así
- `../TIP - Backend/docs/STATE.md` — en qué quedó el trabajo

Están ahí y no duplicados acá a propósito: dos copias del contrato de API se
desincronizan, y una copia desactualizada es peor que no tenerla.

> Para el trabajo del día a día conviene abrir Claude Code en `TIP - Backend`,
> que es donde están los comandos `/retomar` y `/cierre` y desde donde este repo
> queda accesible como directorio adicional. Este archivo existe para las
> sesiones en las que abrís Claude Code directamente acá.
